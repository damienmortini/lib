{
  "name": "lib",
  "private": true,
  "scripts": {
    "start": "server --watch=packages",
    "version": "lerna version",
    "publish": "yarn run version && lerna publish from-package --yes --registry https://npm.pkg.github.com && lerna publish from-package --yes --registry https://registry.npmjs.org",
    "upgrade": "yarn && yarn upgrade-interactive --latest"
  },
  "workspaces": [
    "packages/*",
    "packages/element/*"
  ],
  "devDependencies": {
    "@damienmortini/server": "^1.0.16",
    "eslint": "^7.1.0",
    "eslint-config-google": "^0.14.0",
    "jsdoc": "^3.6.4",
    "lerna": "^3.22.0",
    "yarn": "^1.22.4"
  }
}