{
  "name": "lib",
  "private": true,
  "scripts": {
    "start": "server --watch=packages",
    "publish": "yarn run lerna publish",
    "upgrade": "yarn install && yarn upgrade-interactive"
  },
  "workspaces": [
    "packages/*",
    "packages/element/*"
  ],
  "devDependencies": {
    "@damienmortini/server": "^1.0.16",
    "eslint": "^7.1.0",
    "eslint-config-google": "^0.14.0",
    "jsdoc": "^3.6.4",
    "lerna": "^3.22.0",
    "yarn": "^1.22.4"
  }
}
